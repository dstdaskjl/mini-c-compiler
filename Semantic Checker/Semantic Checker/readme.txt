Using jflex-1.6.1.jar, get Lexer.java file.

When you use a yacc program, type yacc -Jthrows="Exception" -Jextends=ParserImpl -Jnorun -J Parser.y for Parser.java.

You must run one program at a time and close the program for the next run.
Do not run test cases sequentially without closing the program.
